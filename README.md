创建步骤
```
npx create-electron-app my-electron
npm start

npx create-electron-app my-electron --template=typescript
npm install && npm start

```


热启动
```
electron-forge start  后， 如果要重新加载窗口，只需在控制台输入  rs
```
